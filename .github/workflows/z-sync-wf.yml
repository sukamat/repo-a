name: run z-sync

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Generate a token
        id: generate_token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.FG_GITHUB_APP_PRIVATE_KEY }}

      - name: checkout repo content
        uses: actions/checkout@v2 # checkout the repository content

      - name: Set up Git
        run: |
          git config --global user.email "sukamat@adobe.com"
          git config --global user.name "Sunil Kamat"

      - name: Clone Reporting Repository
        run: |
          git clone https://$GITHUB_TOKEN@github.com/orgname/repo-b.git ../repo-b

      - name: Run ls -la
        run: |
          pwd
          ls -la

      - name: Run rsync
        run: |
          echo "Running rsync"
          rsync -av --exclude='fstab.yaml' --exclude='.github' --exclude='.git' --exclude='.idea' ./ ../repo-b/

      - name: Commit and Push Changes to repo-b
        run: |
          cd ../repo-b
          pwd
          ls -la
          echo "git status"
          git status
          echo "git remote -v"
          git remote -v
          echo "git remote set-url for repo-b"
          git remote set-url origin https://github.com/orgname/repo-b.git
          echo "git remote -v"
          git remote -v
          echo "git add ."
          git add .
          echo "git commit -m 'Syncing repo-a to repo-b'"
          git commit -m "Syncing repo-a to repo-b"
          echo "git push origin main"
          git push origin main
        env:
          GITHUB_TOKEN: ${{ steps.generate_token.outputs.token }}
